{
  "name": "physicsrus",
  "version": "1.0.0",
  "description": "Physics\"R\"us is 2D rigid body physics engine and intergrated WYSIWYG editor written in Javascript.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "start": "./node_modules/.bin/browser-sync start -s -w --directory --startPath ./play.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
